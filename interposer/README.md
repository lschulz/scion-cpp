SCION Interposer
================

A SCION Interposer is a library that intercepts an application's calls to the operating system's
network I/O APIs in order to emulate native support for SCION sockets.
